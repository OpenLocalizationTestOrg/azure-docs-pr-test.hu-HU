

A *egyéni* virtuális gép egyszerűen azt jelenti, hogy a virtuális gép egy **kiemelt app** a hello **piactér** mert hello munka nagy meg. Még továbbra is választhat, amelyek tartalmazzák a következő elemek hello konfigurációs beállításokkal:

* Csatlakozás hello virtuális gép tooa virtuális hálózat.
* Hello Azure virtuális gép ügynökének és Azure virtuálisgép-bővítmények telepítése többek között a Antimalware-hez.
* Virtuális gép tooexisting felhőszolgáltatások hello hozzáadása.
* Hello virtuális gép tooan meglévő tárfiók hozzáadásakor.
* Hello virtuális gép tooan rendelkezésre állási hozzáadása beállítva.

<!--
> [!IMPORTANT]
> If you want your virtual machine toouse a virtual network so you can connect tooit directly by host name or set up cross-premises connections, make sure that you specify hello virtual network when you create hello virtual machine. A virtual machine can be configured toojoin a virtual network only when you create hello virtual machine. For details on virtual networks, see [Azure Virtual Network overview](../articles/virtual-network/virtual-networks-overview.md).
>
>
 -->

> [!IMPORTANT]
> Ha azt szeretné, hogy a virtuális gép toouse egy virtuális hálózatot, győződjön meg arról, hogy megadja hello virtuális hálózati hello virtuális gép létrehozásakor.

> * Két előnyeit a virtuális hálózat használatával közvetlenül csatlakozik toohello virtuálisgép- és tooset létesítmények közötti kapcsolatok.

> * A virtuális gép lehet a konfigurált toojoin egy virtuális hálózathoz csak hello virtuális gépet hoz létre. A virtuális hálózatokkal kapcsolatos további információk: [Az Azure Virtual Network áttekintése](../articles/virtual-network/virtual-networks-overview.md).
>
>

## <a name="toocreate-hello-virtual-machine"></a>toocreate hello virtuális gép
