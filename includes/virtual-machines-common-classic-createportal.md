

<span data-ttu-id="bbf1e-101">A *egyéni* virtuális gép egyszerűen azt jelenti, hogy a virtuális gép egy **kiemelt app** a hello **piactér** mert hello munka nagy meg.</span><span class="sxs-lookup"><span data-stu-id="bbf1e-101">A *custom* virtual machine simply means a virtual machine that you create using a **Featured app** from hello **Marketplace** because it does much of hello work for you.</span></span> <span data-ttu-id="bbf1e-102">Még továbbra is választhat, amelyek tartalmazzák a következő elemek hello konfigurációs beállításokkal:</span><span class="sxs-lookup"><span data-stu-id="bbf1e-102">Yet, you can still make configuration choices that include hello following items:</span></span>

* <span data-ttu-id="bbf1e-103">Csatlakozás hello virtuális gép tooa virtuális hálózat.</span><span class="sxs-lookup"><span data-stu-id="bbf1e-103">Connecting hello virtual machine tooa virtual network.</span></span>
* <span data-ttu-id="bbf1e-104">Hello Azure virtuális gép ügynökének és Azure virtuálisgép-bővítmények telepítése többek között a Antimalware-hez.</span><span class="sxs-lookup"><span data-stu-id="bbf1e-104">Installing hello Azure Virtual Machine Agent and Azure Virtual Machine Extensions, such as for antimalware.</span></span>
* <span data-ttu-id="bbf1e-105">Virtuális gép tooexisting felhőszolgáltatások hello hozzáadása.</span><span class="sxs-lookup"><span data-stu-id="bbf1e-105">Adding hello virtual machine tooexisting cloud services.</span></span>
* <span data-ttu-id="bbf1e-106">Hello virtuális gép tooan meglévő tárfiók hozzáadásakor.</span><span class="sxs-lookup"><span data-stu-id="bbf1e-106">Adding hello virtual machine tooan existing Storage account.</span></span>
* <span data-ttu-id="bbf1e-107">Hello virtuális gép tooan rendelkezésre állási hozzáadása beállítva.</span><span class="sxs-lookup"><span data-stu-id="bbf1e-107">Adding hello virtual machine tooan availability set.</span></span>

<!--
> [!IMPORTANT]
> If you want your virtual machine toouse a virtual network so you can connect tooit directly by host name or set up cross-premises connections, make sure that you specify hello virtual network when you create hello virtual machine. A virtual machine can be configured toojoin a virtual network only when you create hello virtual machine. For details on virtual networks, see [Azure Virtual Network overview](../articles/virtual-network/virtual-networks-overview.md).
>
>
 -->

> [!IMPORTANT]
> <span data-ttu-id="bbf1e-108">Ha azt szeretné, hogy a virtuális gép toouse egy virtuális hálózatot, győződjön meg arról, hogy megadja hello virtuális hálózati hello virtuális gép létrehozásakor.</span><span class="sxs-lookup"><span data-stu-id="bbf1e-108">If you want your virtual machine toouse a virtual network, make sure that you specify hello virtual network when you create hello virtual machine.</span></span>

> * <span data-ttu-id="bbf1e-109">Két előnyeit a virtuális hálózat használatával közvetlenül csatlakozik toohello virtuálisgép- és tooset létesítmények közötti kapcsolatok.</span><span class="sxs-lookup"><span data-stu-id="bbf1e-109">Two benefits of using a virtual network are connecting directly toohello virtual machine and tooset up cross-premises connections.</span></span>

> * <span data-ttu-id="bbf1e-110">A virtuális gép lehet a konfigurált toojoin egy virtuális hálózathoz csak hello virtuális gépet hoz létre.</span><span class="sxs-lookup"><span data-stu-id="bbf1e-110">A virtual machine can be configured toojoin a virtual network only when you create hello virtual machine.</span></span> <span data-ttu-id="bbf1e-111">A virtuális hálózatokkal kapcsolatos további információk: [Az Azure Virtual Network áttekintése](../articles/virtual-network/virtual-networks-overview.md).</span><span class="sxs-lookup"><span data-stu-id="bbf1e-111">For details on virtual networks, see [Azure Virtual Network overview](../articles/virtual-network/virtual-networks-overview.md).</span></span>
>
>

## <a name="toocreate-hello-virtual-machine"></a><span data-ttu-id="bbf1e-112">toocreate hello virtuális gép</span><span class="sxs-lookup"><span data-stu-id="bbf1e-112">toocreate hello virtual machine</span></span>
